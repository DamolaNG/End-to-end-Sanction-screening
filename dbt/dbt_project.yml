name: sanctionsight
version: 1.0.0
config-version: 2

profile: sanctionsight

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages", "logs"]

models:
  sanctionsight:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

