sanctionsight:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'sanctionsight') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'sanctionsight') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'sanctionsight') }}"
      schema: public
      threads: 4

